const path = require('path');
const fs = require('fs');
const { csvParser } = require('../katas/csvParser/csvParser');

describe('csvParser()', () => {
  test('returns parsed data as json format', async () => {
    const data = await csvParser();
    
  });
});
